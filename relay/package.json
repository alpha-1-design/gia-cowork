{
  "name": "unimind-relay",
  "version": "1.0.0",
  "description": "Unimind relay — routes messages between GIA devices (desktop + mobile) sharing a unimindId. Run it on the always-on desktop or any VPS; both ends point at it.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
